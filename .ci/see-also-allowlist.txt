# See Also audit allowlist — entries grandfathered as known-broken.
# Format: <source-path>|<broken-ref>
#
# As of S3 (2026-04-27) the allowlist is EMPTY — the project has zero broken
# `## See Also` references. The S2 baseline of 62 broken refs has been fully
# resolved (40 substituted to existing equivalents, 24 dropped as orphans).
# This file is preserved so future drift can be temporarily allowlisted while
# a real fix is staged.
